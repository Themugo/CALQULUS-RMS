# Dependencies
node_modules
.pnp
.pnp.js

# Build output
dist
dist-ssr

# Environment files — NEVER commit real credentials
.env
.env.local
.env.*.local
.env.production
.env.vercel
# Use .env.example as the template

# Bun lockfiles (this is an npm project)
bun.lock
bun.lockb

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

# Zip archives (use git bundles for deployment)
*.zip
*.tar.gz

# OS files
Thumbs.db
ehthumbs.db
Desktop.ini

# Capacitor build artifacts
android/
ios/
.env*.local

# Vercel project metadata
.vercel/

# Supabase CLI temp files — contain real project ref, pooler URL, and version pins.
# These are regenerated by `supabase link` and must never be committed.
supabase/.temp/

# Stale backup/phase folders (historical snapshots, not active code)
batch2/
batch3/
final-batch/
final-ui/
final100/
fixes/
landing/
mega/

# Leftover artifacts
main
fix_all.cjs

# Backup directories from hotfix sessions
**/*.bak/
*.bak

# Playwright test artifacts
playwright-report/
test-results/
