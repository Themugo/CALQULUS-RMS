apiVersion: v1
kind: Service
metadata:
  name: rentflow-canary
  labels:
    app: rentflow
    track: canary
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 5173
    protocol: TCP
    name: http
  selector:
    app: rentflow
    track: canary
